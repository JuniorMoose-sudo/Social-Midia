## Identificação de variáveis de cor do tema atual

As variáveis CSS atuais usadas no projeto são:

- `--green`: cor primária usada em `.btn-primary`, `.wpp-btn`, `.quiz-next:hover` (#25D366)
- `--green-dark`: cor secundária para fundos e destaques como `.nivel-avancado`, `.score-ring` (#2C1730)
- `--green-mid`: cor intermediária para bordas e elementos de destaque como `.dep-result`, `.sobre-photo` (#4A2940)
- `--gold`: cor de acento para chamadas de atenção como `.vagas-display .vagas-num`, `.section-label` (#D6AE62)
- `--pink`: cor de destaque suave em métricas e feedbacks como `.metricas > * .color` (#E2A1B8)
- `--red`: cor de alerta/urgência em métricas como `.metricas > * .color` (#FF6B6B)

✅ Validação automática garante que:
- Nenhum valor hexadecimal inconsistente aparece (ex: `#c86e8a` não é usado);
- Todas as cores acima estão presentes e alinhadas entre README, CSS e JS;
- Não há placeholders como `# TODO Sprint` ou `fallback automático`.